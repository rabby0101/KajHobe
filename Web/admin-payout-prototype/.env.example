# Copy to .env and fill in. NEVER commit .env (it holds the service role key).
SUPABASE_URL=https://xatlqnbrvgukuqewsxux.supabase.co
# Supabase Dashboard -> Project Settings -> API -> service_role secret.
# This key bypasses RLS — keep it server-side, localhost only.
SERVICE_ROLE_KEY=
# Optional, defaults to 4000
PORT=4000
