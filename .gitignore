# macOS
.DS_Store

# Xcode per-user state
*.xcuserstate
xcuserdata/

# Build artifacts (generated — never commit)
iOS/build/
**/DerivedData/
Android/app/release/
Android/app/build/
node_modules/

# Admin panel — localhost-only, must never be pushed to GitHub
Web/admin-payout-prototype/
